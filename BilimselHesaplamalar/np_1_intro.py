import numpy as np
#they are both ndarray
#the only difference is shape
a=np.array([0,1,2,3])
b=np.array([[1,2],[3,4]])
print(a,a.shape) #ilk önce a'yı sonra da a'nın shapeini yazdırmış olduk.4 sütunlu matris olduğunu yazdırdı.
print(b,b.shape) #2Ye 2 matris olduğunu yazdırdı.ama şu an array olarak tanımladığmız için array muamelesi yapıyor.
